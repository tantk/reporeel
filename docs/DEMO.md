# RepoReel — Demo Day Script (3-Act)

**Total runtime:** ~3.5 minutes

---

## Act 1 — Introduce the project (0:00 – 1:05)

**On screen:** Play `outputs/reporeel--reporeel/final.mp4`

This is RepoReel's own RepoReel-generated video — a HeyGen Avatar IV presenter (Madison) narrating, alongside Hyperframes-composed slides:
- **Scene 1 (repo card)**: "RepoReel — Turn any GitHub repo into a presentable video walkthrough" + stats panel (3 scenes / ~3 min / $0 infra)
- **Scene 2 (pipeline)**: Animated 4-step pipeline — Fetch → Plan → Render → Assemble
- **Scene 3 (terminal)**: Mock terminal typing out `/reporeel <github-url>` and showing the pipeline running

**Why we open with this:** the video itself proves the product. The avatar is HeyGen. The "slides" beside it are Hyperframes-rendered. The narration was written by Claude during a skill run. The whole thing was produced by running `/reporeel` on RepoReel's own repo.

**Say after it ends:**
> "Everything you just saw was generated by running RepoReel on RepoReel's own repo. HeyGen Avatar IV rendered the presenter. Hyperframes composed the slides and the layout. Claude wrote the narration during the slash-command session. No human edited the video — only the spec for what RepoReel does."

---

## Act 2 — Show it works on any project (1:05 – 2:20)

**On screen:** Play `outputs/heygen-com--hyperframes/final.mp4`

Same template, different repo. We point RepoReel at `heygen-com/hyperframes` — HeyGen's own flagship open-source project — and the skill autogenerates:
- **Scene 1**: Hyperframes repo card (18.4k stars, Apache 2.0, "Write HTML. Render video.")
- **Scene 2**: HTML → MP4 architecture (HTML composition → Headless Chrome → Frame capture → FFmpeg)
- **Scene 3**: Terminal showing `npx hyperframes init my-video → npm run dev → npm run render`

**Say after it ends:**
> "Same skill. Different repo. The script, the slides, the avatar — all autogenerated by Claude from the README. The whole video is a Hyperframe — meaning the final MP4 was rendered by the same Hyperframes engine the video is talking about. There's a nice symmetry there."

---

## Act 3 — Live demo (2:20 – 3:00)

**On screen:** Claude Code window

**Action:**
```
/reporeel https://github.com/vercel/next.js
```

**What happens live:**
- Claude WebFetches the repo metadata + README in ~5s
- Claude prints a 3-scene plan in chat
- Approval gate appears: `Approve? (y / edit / no)`
- Type `y`
- Render kicks off (~3-4 min — we'll cut early)

**Say while it runs:**
> "What I just typed — that's the entire user-facing API. One slash command. The skill is a 200-line markdown file installed in my Claude Code session. From here, Claude does everything: fetches the repo, plans the narration, renders the avatar through HeyGen's Avatar IV API, then assembles the final video with Hyperframes."

**At ~2:50, cut early:**
> "Renders take a couple of minutes — that's HeyGen processing. The output looks exactly like what you saw in Acts 1 and 2."

---

## Closing pitch (3:00 – 3:30)

**Say (rapid-fire, hit each beat):**

> "**Distribution:** Anthropic Skills marketplace. The product is a single markdown file plus a Bash helper plus a Hyperframes composition. Zero infra cost.
>
> **Both tracks:** Product — every OSS maintainer, every YC founder, every dev influencer needs this. Agent — the pipeline you saw is a real, visible orchestration: WebFetch → reasoning → REST API → Hyperframes engine. Auditable in 200 lines of markdown.
>
> **HeyGen integration:** We use Avatar IV for the presenter and Hyperframes as the composition framework. We're not bolting HeyGen on the side — both products are the load-bearing layers of the output.
>
> **Monetization:** Free tier uses public Photo Avatars. Enterprise: bring-your-own avatar, brand kit, custom voice. $99/mo per company. Releases turn into videos automatically."

---

## Fallback if live demo (Act 3) fails

If `/reporeel` doesn't trigger correctly in front of judges:

1. Say: "I'll skip the live run — you've already seen two real outputs. Let me walk you through the pipeline instead."
2. Open `skills/reporeel/SKILL.md` in your editor; scroll through the 7 workflow steps
3. Continue to Closing pitch

If both pre-baked MP4s fail to load:
- Open the raw scene MP4s in browser tabs (`outputs/<repo>/scenes/intro.mp4` etc.)
- Talk through what the Hyperframes layer would do (point at composition file `hyperframes-build/index.html`)

---

## Submission package

**GitHub repo:** https://github.com/tantk/reporeel (currently private — switch to public before submission)

**Key files to point judges at:**
- `skills/reporeel/SKILL.md` — the slash-command workflow (the product)
- `scripts/render-scenes.sh` — HeyGen Avatar IV integration (REST API + polling + caching)
- `hyperframes-build/index.html` — the Hyperframes composition (HTML → MP4 deck with avatar + slides)
- `reference/default-avatar.json` — Photo Avatar selection (Madison)

**Demo artifacts:**
- `outputs/reporeel--reporeel/final.mp4` — Act 1 (meta video, ~63s, 12MB)
- `outputs/heygen-com--hyperframes/final.mp4` — Act 2 (HeyGen repo video, ~75s, 15MB)

**Note:** `outputs/` is gitignored. For the submission, either upload the two final MP4s to YouTube/Vimeo and link them, or zip the outputs folder and attach.

---

## Submission writeup

**Project name:** RepoReel

**One-liner:** A Claude Code skill that turns any GitHub URL into a presentable video walkthrough — HeyGen Avatar IV narrates, Hyperframes composes.

**HeyGen features used:**
- **Avatar IV** (`POST /v3/videos` with `f20cdc89e0ec4b61bbe453d73019a997` — Madison Photo Avatar) for the spoken presenter
- **Hyperframes** (`@hyperframes` framework, open-source) as the composition layer — HTML → MP4 via headless Chrome + FFmpeg

**Tracks:** Product Track primary, Agent Track eligible.

**Why both tracks:**
- **Product:** Ships as a markdown skill on Anthropic's marketplace. Zero infra cost. Anyone with Claude Code + a HeyGen API key installs and uses it. Path to revenue: enterprise BYO-avatar tier.
- **Agent:** The skill IS a visible orchestration pipeline — Claude wires WebFetch → reasoning → Bash → HeyGen REST → Hyperframes render. The "agent stack" is auditable in plain markdown.

**Differentiation from existing work:**
- `joshua-heygen/viz-pack` (similar Claude-skill direction) outputs static visuals (diagrams, infographics). RepoReel outputs **narrated avatar video** — fundamentally different deliverable.
- `heygen-com/website-to-hyperframes` converts a URL into a Hyperframe. RepoReel is repo-focused, leverages README + metadata for structured scene planning rather than visual scraping.

**Why HeyGen wins by us winning:** Every RepoReel run consumes HeyGen Avatar IV credits *and* renders via Hyperframes. Both products get usage in every video — and every published video acts as a marketing demo for both.
